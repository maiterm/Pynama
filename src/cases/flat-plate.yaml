---
name: FlatPlate
save-dir: test-flat-plate-5

domain:
  ngl: 3
  box-mesh:
    nelem: [5,5,5]
    lower: [0,0,0]
    upper: [1,1,1]

time-solver:
  max-steps: 300
  start-time: 0.001
  end-time: 100

material-properties:
  rho: 0.5
  mu: 0.01

boundary-conditions:
  custom-func:
    name: flat_plate
    attributes: ['velocity', 'vorticity', 'alpha']

initial-conditions:
  custom-func:
    name: flat_plate
    attributes: ['velocity', 'vorticity', 'alpha']

tests:
  custom-func:
    name: flat_plate
    attributes: ['velocity', 'vorticity', 'alpha']